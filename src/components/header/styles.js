import styled from 'styled-components';

const ContainerHeader = styled.div`
	padding: 2rem;
	display: flex;
	flex-direction: row;
	justify-content: space-between;
`;
const StyledIconMenu = styled.img`
	width: 20px;
`;
export { ContainerHeader, StyledIconMenu };
